Algoritmo "Atividade 3"

Var

contador,multiplo,resto:inteiro

Inicio

   enquanto (contador <= 100) faca
   escreval(contador)

   contador <- contador +1
   
   resto <- contador % 10
    
    se resto = 0 então
      escreval(contador, " É Múltiplo de 10.")

fimSe

fimEnquanto
Fimalgoritmo